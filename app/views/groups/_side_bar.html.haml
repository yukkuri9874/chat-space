.chat-side
  .chat-side__header
    .chat-side__header--user
      = current_user.name
    .chat-side__header__icon
      = link_to new_group_path do
        = fa_icon 'edit', class: "chat-side__header__icon--btn"
      = link_to edit_user_path(current_user) do
        = fa_icon 'cog', class: "chat-side__header__icon--btn"
  .chat-side__groups
    .chat-side__groups__group
      - current_user.groups.each do |group|
        = link_to '#' do  
          .chat-side__groups__group--group-name
            = group.name
          .chat-side__groups__group--latest-message
            グループメッセージがありません。